import SolutionsSections from "@/components/solutions";

export const metadata = {
  title: "Solutions — Interval 360",
  openGraph: { title: "Solutions — Interval 360" },
  twitter: { title: "Solutions — Interval 360" },
};

export default function SolutionsPage() {
  return <SolutionsSections />;
}
