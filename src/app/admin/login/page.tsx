import LoginLayout from "@/components/LoginLayout";

export const metadata = { title: "Admin Sign In — Interval 360" };

export default function AdminLoginPage() {
  return <LoginLayout variant="admin" />;
}
