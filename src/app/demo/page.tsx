import DemoChat from "@/components/DemoChat";

export const metadata = {
  title: "Demo — Interval 360",
  robots: "noindex, nofollow",
};

export default function DemoPage() {
  return <DemoChat />;
}
