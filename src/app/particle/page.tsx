import ChatLanding from "@/components/ChatLanding";

export const metadata = {
  title: "Start Feedback | Interval 360",
  description: "AI-guided 360 feedback conversation — takes around 5 minutes.",
};

export default function ParticlePage() {
  return <ChatLanding />;
}
